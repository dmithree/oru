# tasks-hub — Phase 0 (store + events only, no ingestion/render yet)

# Optional: agent will run without these in Phase 0
telegram_bot_token=
telegram_chat_id=

# Reserved for later phases (ingestion, adaptive filtering)
anthropic_api_key=
linear_api_key=

http_host=0.0.0.0
http_port=8004
